# demo-audit-log-diff-collection
Repository is a ansible module that is part of the demo

To build the collection run the commands:
$ ansible-galaxy collection build
$ ansible-galaxy collection install ddimatos-zos*.tar.gz
$ ansible-galaxy collection list
